#include "main.h"

/**
 * mul - Multiplies two integers
 * @x: The firdt number to multiply
 * @y: The second number to multiply with first number
 * Return: The result of multiplication between the x param and y param
 */

int mul(int x, int y)

{
	return (x * y);
}
